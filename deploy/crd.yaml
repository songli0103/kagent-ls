---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: agents.agent.demo.io
spec:
  group: agent.demo.io
  names:
    kind: Agent
    listKind: AgentList
    plural: agents
    singular: agent
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.observedGeneration
      name: Generation
      type: integer
    - jsonPath: .spec.task.tool
      name: Tool
      type: string
    - jsonPath: .status.jobRef.name
      name: Job
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Agent is the Schema for the agents API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AgentSpec defines the desired state of Agent.
            properties:
              config:
                description: Config carries tool-specific configuration.
                properties:
                  namespace:
                    description: |-
                      Namespace restricts the inspection scope to a single namespace.
                      When empty, cluster-wide resources are listed.
                    type: string
                type: object
              runtime:
                description: Runtime is the container runtime to execute the task
                  in.
                properties:
                  image:
                    description: Image is the container image that the Operator will
                      run inside the Job.
                    type: string
                required:
                - image
                type: object
              task:
                description: Task is the unit of work to execute.
                properties:
                  args:
                    additionalProperties:
                      type: string
                    description: |-
                      Args are tool-specific parameters forwarded to the runtime as
                      AGENT_TASK_ARG_<KEY> environment variables. Keys must be unique.
                    type: object
                  tool:
                    description: |-
                      Tool selects which tool the agent should execute.
                      Currently the only supported tool is "cluster-inspect".
                    enum:
                    - cluster-inspect
                    type: string
                required:
                - tool
                type: object
            required:
            - runtime
            - task
            type: object
          status:
            description: AgentStatus defines the observed state of Agent.
            properties:
              conditions:
                description: Conditions is a list of observed conditions.
                items:
                  description: AgentCondition describes a condition the Agent has
                    transitioned through.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human-readable description.
                      type: string
                    reason:
                      description: Reason is a short machine-readable explanation.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type of the condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              finishedAt:
                description: FinishedAt is the time execution terminated.
                format: date-time
                type: string
              jobRef:
                description: JobRef is the Job that is executing the Agent.
                properties:
                  name:
                    description: Name is the Job name in the same namespace as the
                      Agent CR.
                    type: string
                type: object
              observedGeneration:
                description: |-
                  ObservedGeneration is the spec generation last processed by the
                  controller. The controller only re-runs when metadata.generation
                  differs from this value.
                format: int64
                type: integer
              phase:
                description: Phase reports the high-level execution state.
                type: string
              result:
                description: Result holds the structured payload produced by the runtime.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              startedAt:
                description: StartedAt is the time execution began.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
