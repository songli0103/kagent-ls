---
# Per-namespace ServiceAccount used by the agent runtime Deployment.
# The operator does not create this — the user creates one in whatever
# namespace they apply their Agent CR. The ClusterRoleBinding above grants
# any SA with this name the read-only cluster scope it needs.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: agent-runtime
  namespace: default
